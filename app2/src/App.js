import LocalButton from './Button';
import React from 'react';
import { Grid, Row, Col, Paper } from '@mui/material';

const App = () => (
  <Grid container>
    <Grid item>
      <Paper>
        Charting Container
      </Paper>
    </Grid>
  </Grid>
);

export default App;
